# 개발 기록 — Windows 네이티브 지원을 시도하며

이 문서는 "Windows 카카오톡도 macOS처럼 앱 DB를 자동으로 읽어 검색할 수 있을까?"를 파고든 과정을, **성공한 방법과 실패한 방법을 모두** 투명하게 정리한 기술 기록입니다.

> **윤리·법적 전제**: 아래 모든 분석은 **본인 소유 PC에서 본인 계정으로 로그인된 카카오톡**을 대상으로 한 것입니다. 타인의 대화를 열람·수집하는 용도가 아닙니다. 그리고 아래 "성공한 방법"에 해당하는 **실제 추출 도구는 프라이버시·법적 이유로 이 저장소에 포함하지 않습니다.** 이 문서는 왜 Windows 자동 직독이 어려운지, 어떤 접근이 막히는지에 대한 연구 기록입니다.

---

## 1. macOS와 Windows의 근본 차이

- **macOS**: 카카오톡 로컬 DB는 SQLCipher로 암호화되어 있고, 그 키 유도 방식(기기 UUID + user_id)이 공개적으로 알려져 있습니다. 그래서 원본 katok이 자동 복호화합니다. "잠긴 문 + 공개된 열쇠 제작법".
- **Windows**: 대화 로그(`chat_data\chatLogs_*.edb`)는 자체 방식으로 암호화되어 있고, 키가 앱 안에 숨겨져 있습니다. 게다가 실행 파일이 상용 보호기로 이중 보호됩니다. "이중 잠긴 상용 금고".

---

## 2. 가장 큰 장벽: Themida 보호

`KakaoTalk.exe`(약 87MB)의 PE 섹션을 확인해 보면 `.themida`, `.boot`, `.vm_sec` 같은 섹션이 있고, **디스크 상에서 사람이 읽을 수 있는 문자열이 사실상 하나도 없습니다**(예: "PRAGMA", "sqlite3" 같은 SQLite 흔적조차 안 보임).

이는 **Themida**라는 상용 보호기의 특징입니다:

- **코드 가상화**: 원래 x86 코드가 Themida 자체 가상머신 바이트코드로 변환되어, 정적 디스어셈블(IDA/Ghidra 등)로는 로직을 바로 읽을 수 없습니다.
- **런타임 언팩**: 실제 코드/문자열은 실행 시점에 메모리에서 복원됩니다.
- **안티 디버그 / 안티 후킹**: 디버거 부착·함수 후킹을 방해합니다.

정리하면, **Windows 카카오톡의 DB 복호화 로직은 Themida VM 안에서 돌아가기 때문에, 키를 뽑거나 복호화 함수를 후킹하는 "정공법"이 근본적으로 막혀 있습니다.** 이것이 이 프로젝트에서 가장 어려운 부분이었고, macOS와 결정적으로 다른 점입니다.

---

## 3. 시도했지만 막힌 방법들 (연구 기록)

키를 얻거나 복호화 지점을 잡으려는 여러 접근을 시도했고, 아래는 모두 **막혔습니다.** (그래서 재현용 코드는 싣지 않고, 왜 막히는지만 기록합니다.)

1. **프로세스 메모리에서 키 직접 탐색**
   메모리 전체를 훑어 AES 키 후보를 찾고, 실제 암호문 페이지로 검증. → 키가 원시 형태로 상주하지 않음(보호기/휘발). 실패.

2. **Windows 표준 암호화(CNG/bcrypt) 후킹**
   앱이 OS의 `bcrypt`를 쓴다면 키가 그리로 지나갈 것이라 보고 후킹. → 잡히는 키는 전부 **네트워크(TLS)·텔레메트리용**이었고 DB 키가 아니었음. 즉 DB 복호화는 OS 표준 경로를 안 씀. 실패.

3. **앱 내부 AES 키 셋업/복호화 함수 후킹**
   실행 중 메모리에서 AES-NI 명령 위치를 찾아 키 셋업 함수를 후킹. → 앱 시작 시점에 후킹을 걸면 보호기가 거부("intercept 불가"), 나중에 붙으면 이미 지나간 뒤. 잡힌 키도 DB용이 아니었음. Themida 안티후킹에 막힘. 실패.

4. **정적 리버스 엔지니어링**
   IDA/Ghidra로 코덱·키 유도 함수를 추적. → Themida VM devirtualize가 선행되어야 하는 초고난도 작업. 인라인 범위에서 비현실적.

**교훈**: 보호기가 코드를 지키는 한, 키를 훔치거나 함수를 후킹하는 접근은 계속 막힙니다.

---

## 4. 개념적으로 가능한 우회 (그러나 이 저장소에는 미포함)

보호기가 코드는 지켜도, **앱이 실제로 대화를 화면에 보여주려면 결국 복호화된 데이터를 메모리에 펼쳐야 합니다.** 즉 "코드"가 아니라 "결과 데이터"를 보는 접근은 보호기와 무관하게 가능합니다. 디지털 포렌식에서 잘 알려진 원리입니다.

이 방향은 개념적으로 성립하지만, 이렇게 얻는 데이터에는 **그룹 대화 속 다른 사람들의 메시지(동의하지 않은 제3자)** 가 포함됩니다. 이를 손쉬운 도구로 만들어 **공개 배포**하는 것은:

- 대한민국 **통신비밀보호법**(타인 통신 열람) 저촉 소지,
- **저작권법 §104-2**(기술적 보호조치 무력화 도구 배포) 저촉 소지,
- 제3자 프라이버시 침해

가 있으므로, **해당 추출 도구/재현 절차는 이 공개 저장소에 포함하지 않습니다.** 본인 데이터에 한정한 개인 연구 목적으로만 다뤄야 합니다.

---

## 5. 그래서 이 저장소가 제공하는 것

위와 같은 이유로, Windows에서는 **합법적이고 안전한 경로**만 제공합니다:

- 카카오톡 공식 **"대화 내보내기"** 로 만든 `.txt`를 파싱하는 `--source txt` 어댑터.
- 원본 katok의 검색 엔진(keyword/BM25/semantic) 위에서 그대로 검색.

내보내기 방식은 자동 직독보다 손이 더 가지만(방마다 내보내야 함), 앱 보호를 우회하지 않으며 내 데이터만 다루므로 안전합니다.

---

## 6. 요약

| 항목 | 상태 |
|---|---|
| Windows 빌드 포팅 | 완료 (순정 SQLite, OpenSSL/perl 불필요) |
| `--source txt` 검색 | 완료 (PC/모바일 내보내기 포맷 파싱) |
| macOS 자동 직독 | 원본 그대로 유지 |
| Windows `katok send` / `peek` | 공식 `KakaoTalk.exe` UI Automation (포커스 불필요) + 열린 창 말풍선 읽기 (프로토콜 클라이언트 아님) |
| Windows 자동 직독 | **미제공** — Themida 보호 + 제3자 프라이버시/법적 이유 |

Windows 카카오톡의 강한 보호는 사용자 프라이버시 관점에서는 오히려 바람직한 설계입니다. 이 프로젝트도 그 취지를 존중하여, 내 데이터를 내가 검색하는 합법 경로에 집중합니다.
